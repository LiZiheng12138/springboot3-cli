server:
  port: 8788
spring:
  datasource:
    dynamic:
      primary: primary
    primary:
      jdbc-url: jdbc:mysql://localhost:3306/auth?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
      username: root
      password: Aa123456!
      driver-class-name: com.mysql.cj.jdbc.Driver
#      type: com.zaxxer.hikari.HikariDataSource  # 必须指定

  security:
    oauth2:
      authorizationserver:
        issuer: http://localhost:8788

mybatis-flex:
  global-config:
    print-sql: true
  #    logic-delete-column: status
  mapper-locations: classpath*:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

app:
  jwt:
    secret: very-very-secret-change-me-to-long-random-string-256bits
    access-token-valid-seconds: 1800   # 30 min
    refresh-token-valid-days: 30